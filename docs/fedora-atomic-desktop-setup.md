# FEDORA: Atomic Desktop Setup

## Install Video Codecs

```bash
rpm-ostree install ffmpeg-free libavcodec-freeworld libva-intel-driver
```

## Install Distrobox

```bash
rpm-ostree install distrobox
```
